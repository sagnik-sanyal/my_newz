import 'package:flutter/widgets.dart';

/// App Colors used in the application
sealed class AppColors {
  const AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFF0C54BE);
  static const Color secondary = Color(0xFF303F60);

  // Shade Colors
  static const Color primaryLight = Color(0xFFF5F9FD);
  static const Color grey = Color(0xFFCED3DC);
}
